services:
  app:
    build: .
    ports:
      - '3000:3000'
    environment:
      PORT: 3000
    deploy:
      resources:
        limits:
          cpus: '0.50'
          memory: 256M
        reservations:
          cpus: '0.25'
          memory: 128M
